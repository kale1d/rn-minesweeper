import { Dimensions } from "react-native";


export const MAX_COLS = 10;
export const MAX_ROWS = 10;
export const MINES = 10;
export const width = Dimensions.get('screen').width - 16;
export const height = width;
